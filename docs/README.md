# Documentation

Supplements the root [README.md](../README.md).

| Document | Purpose |
|----------|---------|
| [detection-bot-template.md](detection-bot-template.md) | How Detection Bot maps generated rules to `ScheduledRuleTemplate.yaml` |

All CLI tools write artefacts under **`output/<tool_name>/`** at the repository root (see README “Project structure”).

**Threat digest extras:** Optional `company_profile.yaml` (see `company_profile.example.yaml`) tailors LLM and HTML output; HTML reports are written next to the markdown digest.
